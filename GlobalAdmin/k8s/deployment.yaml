apiVersion: apps/v1
kind: Deployment
metadata:
  name: global-admin
  labels:
    app: global-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: global-admin
  template:
    metadata:
      labels:
        app: global-admin
    spec:
      containers:
      - name: global-admin
        image: registry.digitalocean.com/scanrev/global-admin:latest
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: ConnectionStrings__MongoDB
          valueFrom:
            configMapKeyRef:
              name: rev-config
              key: MongoDbConnectionString
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /login
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /login
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: global-admin
spec:
  selector:
    app: global-admin
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: global-admin
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - admin.scanrev.com
    secretName: global-admin-tls
  rules:
  - host: admin.scanrev.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: global-admin
            port:
              number: 80
